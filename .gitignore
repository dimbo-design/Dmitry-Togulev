# Project instructions — local only
CLAUDE.md

# Personal context — never committed (private)
me-context.md

# Internal CV/job-search tooling — local only, не в публичный репо
resume-pdf.html
resume-fields.html

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Reference drop zone — never committed
user_input/

# Staging and memory folders
uploads/
memory/

# Editors
.vscode/
.idea/
*.swp
*.swo

# Misc
Thumbs.db
